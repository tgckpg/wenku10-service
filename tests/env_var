#!/bin/bash

ACCOUNT="penguin"
PASSWD="1234456789"
EMAIL="example@example.com"
RLANG="en-US"
ACCESS_TOKEN="This is the access token"
COOKIE="cookie.tmp"
SEND_COOKIE=false
PUBKEY="HUTNEHNOHUNHENHUTNHENU"

if [ -f uuid.tmp ]; then
	UUID=$( cat uuid.tmp )
fi
